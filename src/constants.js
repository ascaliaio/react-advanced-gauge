export const MAX_ANGLE = 100;
export const MIN_ANGLE = -100;
export const MIN_HEIGHT = 200;

// Default value path color
export const DEFAULT_VALUE_PATH_COLOR = '#555';

export const COLORS = {
  RED: '#e74c3c',
  YELLOW: '#fff000',
  GREEN: '#2ecc71',
};

export const DEFAULT_COLORS = [
  COLORS.GREEN,
  COLORS.YELLOW,
  COLORS.RED,
];
